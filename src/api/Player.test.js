import axios from 'axios';
import PlayerApi from './Player';
jest.mock('axios');

/* describe('PlayerApi', () => {
  describe('get', () => {
    it('calls api', async () => {
        expect(true).toEqual(true);
        await PlayerApi.get(2);
        
        expect(axios.get).toHaveBeenCalledWith('');
      });
  });
}); */